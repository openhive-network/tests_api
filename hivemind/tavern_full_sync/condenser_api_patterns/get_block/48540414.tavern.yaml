---
  test_name: Hivemind condenser_api.get_block 48540414

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_block 48540414
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_block"
          params: [48540414]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:has_valid_response
          extra_kwargs:
            method: "48540414"
            directory: "condenser_api_patterns/get_block"

