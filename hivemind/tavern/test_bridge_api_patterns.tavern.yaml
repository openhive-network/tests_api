---
  test_name: Hivemind bridge_api.account_notifications patterns test

  marks:
    - patterntest
    - xfail #shouldn't pass on hive 3 

  includes:
    - !include common.yaml

  stages:
    - name: account_notifications
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.account_notifications"
          params: {"account":"steemit","limit":100}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "account_notifications"
            directory: "bridge_api_patterns"

---
  test_name: Hivemind bridge_api.account_notifications2 patterns test

  marks:
    - patterntest
    - xfail #shouldn't pass on hive 3 

  includes:
    - !include common.yaml

  stages:
    - name: account_notifications2
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.account_notifications"
          params: {"account":"abit","limit":100}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "account_notifications2"
            directory: "bridge_api_patterns"
