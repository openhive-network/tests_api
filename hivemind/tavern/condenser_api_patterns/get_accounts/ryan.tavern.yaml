---
  test_name: Hivemind condenser_api.get_accounts patterns test two 33091

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_accounts
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_accounts"
          params: [["camilla", "ryan-singer"]]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "ryan"
            directory: "condenser_api_patterns/get_accounts"