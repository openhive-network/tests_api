---
  test_name: Hivemind

  marks:
    - patterntest # HF1 multiplied all vests and rshares by 1mln; in case of this post first 3 votes were prior to that, 4th was after and first cashout
                  # happened way further; now that net_rshares are taken from vop, we have actual value used by hived; it makes no difference post HF1

  includes:
    - !include ../../common.yaml

  stages:
    - name: test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_discussions_by_comments"
          params: {"start_author":"admin","limit":1}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            ignore_tags: "<condenser posts>"
